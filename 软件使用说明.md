# Aura-Space 软件使用说明

> 适用对象：黄山派手表端用户（玩家/演示者）与开发调试人员
> 固件版本：四模块完整版（右滑返回）

---

## 第一部分：快速上手

### 1. 开机与主界面

1. 用 Type-C 数据线连接手表与电脑（或充电宝）供电；
2. 电脑上用串口工具（PuTTY）连接手表串口（COMx，**波特率 1000000**），输入：
   ```
   nsh> aura app
   ```
3. 手表屏幕点亮，显示 **Aura-Space 主界面**：四个彩色入口按钮——
   **BeatFlicks（音游）/ Synapse-N（记忆矩阵）/ Aura-Focus（呼吸引导）/ AI Coach（数据教练）**。

### 2. 通用操作

| 操作 | 效果 |
|---|---|
| 点按按钮 | 进入对应模块 / 触发功能 |
| **手指按屏向右滑动**（>1/5 屏宽） | **返回主界面**（所有模块通用） |
| 屏幕底部小字 `swipe right = home` | 返回方式提示 |

---

## 第二部分：四大模块玩法

### 一、BeatFlicks 体感音游

**目标**：音符落到屏幕底部轨道线的瞬间完成对应动作，命中越准得分越高。

**界面元素**：

- 顶部：`SCORE`（得分）、`COMBO`（连击）、`ACC`（准确率）
- 底部三条轨道：**U（上抛·橙色）/ L（左甩·绿色）/ R（右甩·蓝色）**
- 中部：判定闪现 Perfect（金）/ Good（绿）/ Miss（红）
- 屏幕边框橙色脉冲 = 节拍提示（音乐引擎的视觉降级）

**两种玩法（可同时使用）**：

| 方式 | 动作 |
|---|---|
| **体感** | 手持手表：U=抬腕上抛、L=手腕外旋左甩、R=手腕内旋右甩 |
| **触屏** | 音符到达轨道线时直接点按对应轨道按钮 |

**计分规则**：

- Perfect ±100ms：10 分；Good ±500ms：5 分；Miss：0 分且连击清零
- 每满 10 连击，得分倍率 +10%
- 一局 8 个音符，结束显示评级（S/A/B/C/D）并自动开始新一局
- 系统根据你的历史表现（Glicko-2 评分）动态调整后续难度

### 二、Synapse-N 记忆矩阵

**目标**：训练工作记忆。2×2 象限中色点依次闪现，判断"当前色点的**位置+颜色**是否与 N 步之前完全相同"。

**玩法**：

1. 点 `START` 开始（初始 N=2）；
2. 盯住色点闪现（显示 0.85 秒，之后 2 秒作答窗）；
3. 与 N 步前一致 → 点 **MATCH** 按钮；不一致 → 不点（等超时即可）；
4. 反馈：答对绿色 `OK`、答错红色 `X`；
5. **自适应难度**：近 10 轮正确率 ≥75% 自动 N+1（最高 4），≤50% 自动 N-1（最低 1）；
6. 一局 30 轮，结束显示得分与准确率，成绩自动计入五维"工作记忆"。

### 三、Aura-Focus 心流呼吸

**目标**：通过节律呼吸进入放松专注状态。

**玩法**：

1. 点 `START` 开始 4 分钟一节；
2. 跟随圆环：**INHALE 吸气 4 秒**（蓝色圆环扩大）→ **EXHALE 呼气 6 秒**（绿色圆环缩小）；
3. 顶部显示本节剩余时间；
4. 可以随时右滑退出，**已进行的心流时长照常记录**；满 4 分钟自动结束并计入五维"心流维持"。

### 四、AI Coach 数据教练

**查看内容**：

- **Focus Score**：今日专注综合评分（五维加权）；
- **五维条形图**：Reaction 反应速度 / Focus 持续专注 / Memory 工作记忆 / Pressure 压力状态 / Flow 心流维持（最弱项标橙、最强项标绿）；
- **Suggestion**：针对最弱维度的个性化训练建议（本地规则引擎生成，无网络也可使用）。

数据来源：玩 BeatFlicks、Synapse-N、Aura-Focus 的成绩会自动聚合到这里。

---

## 第三部分：开发调试命令（NSH 控制台）

串口连接：PuTTY → COMx → **波特率 1000000** → 回车出现 `nsh>`。

| 命令 | 用途 |
|---|---|
| `aura app` | 启动四模块图形应用（日常使用入口） |
| `aura demo` | 控制台全链路演示（判定/结算/评分打印） |
| `aura imu` | 10Hz 打印 IMU 融合数据（检查传感器是否工作） |
| `aura gesture` | 实时打印识别到的手势与置信度，每 10 秒附采样抖动统计 |
| `aura calib` | 每 250ms 打印三轴角速度窗口峰值（采集真实甩腕数据，用于标定阈值） |
| `aura inject` | 注入 100 组合成波形，输出识别准确率（验收 ≥90%） |
| `aura vib <1-5>` | 播放指定振动模式（马达引脚接通后可用） |

**常用排查**：

| 现象 | 处理 |
|---|---|
| 串口乱码 | 波特率必须是 **1000000**，不是 115200 |
| 屏幕黑但串口正常 | 没有图形程序在跑，输入 `aura app` |
| 手势不识别 | `aura gesture` 看实时事件；`aura calib` 采峰值发回分析 |
| 找不到 COM 口 | 换**能传数据**的 Type-C 线（充电线没有数据线芯）；设备管理器确认 CH340 |
| 烧录失败 | sftool 报错会列出当前可用 COM 口，以列表为准 |

---

## 第四部分：固件升级（给维护者）

```bash
# ① 同步代码（Git Bash）
MSYS_NO_PATHCONV=1 wsl -d Ubuntu-22.04 -u root -e bash -c \
  "cp -rf /mnt/c/Users/notebook/Desktop/Aura-Space/hardware/aura_hw/. /root/vela/apps/aura_hw/"

# ② 增量编译
MSYS_NO_PATHCONV=1 wsl -d Ubuntu-22.04 -u root -e bash -c \
  "export PATH=/root/vela/tools/arm-gnu-toolchain-12.3.rel1-x86_64-arm-none-eabi/bin:\$PATH && \
   cmake --build /root/vela/cmake_out/lckfb_huangshan_pi && \
   cp /root/vela/cmake_out/lckfb_huangshan_pi/nuttx.bin /mnt/d/tools/nuttx.bin"

# ③ 烧录（COM 号以设备管理器为准）
/d/tools/sftool/sftool.exe -c SF32LB52 -p COM6 -b 1000000 \
  --before default_reset --after soft_reset write_flash D:/tools/nuttx.bin@0x12010000
```

> 改了 defconfig / Kconfig 时需要全量重配：先把 `cmake_out` 目录改名，再运行构建脚本（详见 `hardware/真机调测指南.md`）。

---

*文档版本：v1.0（四模块完整版）· 2026-09*
