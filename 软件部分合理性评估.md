# Aura-Space 软件部分合理性评估

> 依据：《心流节拍项目方案报告（第三组）》、`Aura-Space-frontend/`、`Aura_Space_Backend/` 当前代码
> 评估日期：2026-09-19

## 总体结论

**架构方向合理，可作为真机集成基础；但判定参数未统一、判定算法存在正确性缺陷，上板前必须修复。**

软件部分整体与方案报告 2.1 的分层设计（应用层 / 服务层 / 数据层 + EventBus 解耦）一致，前后端通过 Mock/API 双模切换并行开发的做法是合理的工程实践。目前的主要风险不在架构，而在三处"数字对不上"和四个判定逻辑问题。

---

## 一、后端 C 游戏引擎（Aura_Space_Backend）

### 符合方案的点

- JSON 谱面解析（cJSON）、三档判定、连击倍率（每 10 连击 +10%）、S/A/B/C/D 评级，与方案 3.1 设计一致；
- 音符数组一次性 `malloc` 整块内存，避免堆碎片，符合方案"内存池管理"要求的意图；
- `current_index` 单调递增避免每次 O(n) 扫描，思路正确；
- 无 RTOS 依赖，可近乎零修改编译进 openvela（已在 `hardware/aura_hw/engine/` 完成复用）。

### 必须修复的问题

| # | 位置 | 问题 | 严重度 | 建议 |
|---|------|------|--------|------|
| 1 | `game_engine.h:8-9` | **判定窗口三方不一致**：方案 1.2/2.1 写 Perfect ±50ms / Good ±100ms，方案 3.1 详细设计写 ±100ms / ±500ms，代码取了 ±100/±500。方案报告自身即自相矛盾 | 高 | 全队定稿一组参数（建议 ±50/±100，贴合"判定延迟<10ms"的严谨定位），改 2 行宏即可 |
| 2 | `judge_system.c:107` | **只匹配 `current_index` 一个音符**。当 Good 窗口（500ms）大于音符间隔时，手势会被判给时间上更远的队首音符，而不是 \|Δt\| 最小的音符 | 高 | 在窗口内向前/向后扫描取 \|Δt\| 最小者。前端 `beatflicks.js` 的 `gesture()` 已按 delta 排序选最近音符，是正确实现，后端应对齐 |
| 3 | `judge_system.c:61-62` | **`max_possible_score` 用 `max_combo+1` 估算倍率**，Miss 断连后分母仍按虚拟高倍率累计，导致 `accuracy` 失真（不是真实命中率） | 中 | 改为 `accuracy = Σ(perfect×10 + good×5) / (总音符数×10)`，分母与连击解耦 |
| 4 | `judge_system.c:121-122` | **手势类型不匹配直接 Miss 并消费音符**。在 ±500ms 宽窗口下，用户在音符 A 窗口内做了音符 B 的手势会造成"A 错型 Miss + B 超时 Miss"双杀 | 中 | 类型不匹配时不消费音符，只记一次无效输入；或窗口收紧后此问题自然缓解 |
| 5 | `judge_system.c:140,164` | EventBus 广播是 TODO 注释，未实现 | 中 | 已在硬件包 `game_port.c` 用包装方式补齐（无需改后端代码） |
| 6 | `main.c:72-77` | 依赖调用方手动 `free`，上板后长期运行建议改静态音符池 | 低 | 真机版改静态数组（128 音符上限），见 hardware/README |

### 未实现的方案承诺（属排期问题，非缺陷）

谱面动态生成接口、Glicko-2 动态难度、N-Back 状态机、音频预加载。其中 Glicko-2 已在硬件包补了初版（`glicko2.c`，固定 σ 变式），N-Back 状态机仍需补。

---

## 二、FastAPI 后端（api/server.py）

**定位判断：合理。** 它是局域网开发桩（Mock 数据源），不是最终形态——真机阶段其角色由板端 KV 存储 + BLE 同步取代。CORS 配置（禁止 `*` 与 credentials 共存）修得正确。

存在的问题：

1. `/api/dashboard` 返回硬编码数据，无持久化、无聚合逻辑——作为桩可接受，但文档未声明"仅桩"；
2. **接口路径与契约文档不一致**：代码是 `/api/beatmap`、`/api/game_result`，而 `docs/INTEGRATION.md` 契约写的是 `/api/games/beatflicks/chart`、`/api/training/records`。前端 adapter 跟着代码走，文档已过期，三者必须统一一次；
3. **字段语义混用**：`platform-adapter.js:93` 把后端 `radar.reaction`（0–100 分）直接当 `reactionMs`（毫秒）使用，五维雷达分数与物理指标两套语义被混进同一结构，AI 看板展示的"反应速度 80ms"这类数据会是错的。

---

## 三、前端（Aura-Space-frontend）

### 合理的点

- EventBus / PlatformAdapter / FocusEngine 分层与方案一致，模块间无直接依赖；
- 音符渲染用 32 对象池复用，避免每帧创建对象造成 GC 压力，符合方案"避免内存碎片"目标；
- Mock/API 双模 + AI 本地规则降级（`localCoachAnswer`），落实了方案"云端不可用必须降级"的要求；
- `INTEGRATION.md` 契约 + 交接说明写得清楚，且诚实声明了"这是浏览器 Demo，不是可烧录固件/RPK"的边界。

### 问题

1. **Miss 超时判定 145ms（`beatflicks.js:86`）与后端 500ms、方案 100ms 三处不一致**——虽然契约声明"正式判定以引擎为准、前端仅 Mock"，但演示时评委看到的前后端判定手感差异会露馅，建议统一；
2. `BackgroundSampler` 是纯前端状态模型（电量 76 写死），真机后需替换为板端电源管理事件——属预期内工作，交接说明已声明；
3. **最大缺口**：浏览器 Canvas/DOM 无法直接跑在 SF32LB52 上。黄山派 openvela 配置自带 LVGL + LCD + 触摸，UI 需用 LVGL 重写渲染层。建议保留 `PlatformAdapter` 的接口语义和 FocusEngine 的分析逻辑（这两层与 DOM 无关、可移植），只重写呈现层。

---

## 四、与方案报告的功能缺口清单（截至评估日）

| 方案承诺 | 状态 |
|---|---|
| IMU 驱动 / 手势识别 / 振动反馈 | 本次 hardware/ 交付补齐 |
| EventBus（C 端） | 本次补齐（`event_bus.c`） |
| 五维数据模型 + KV 存储 | 本次补齐（`radar_store.c`，kvdb/文件双后端） |
| Glicko-2 | 本次补初版（`glicko2.c`） |
| N-Back C 端状态机 | **缺失**（前端仅有 UI mock） |
| BLE 跨端同步 | **缺失** |
| 音频解码 / 音画同步 | **缺失**（板端有 Class-D PA 硬件可用） |
| 呼吸引导 Aura-Focus 板端实现 | 缺失（前端已有动画原型） |
| LVGL 真机 UI | **缺失，上板最大工作量** |

## 五、方案报告本身的文字问题（建议修订）

1. Perfect/Good 窗口在 1.2、2.1（±50/±100）与 3.1（±100/±500）之间自相矛盾；
2. 3.1 谱面字段列出四种音符类型（上抛/下压/左甩/右甩），但手势只有三种，"下压"悬空；
3. "拍腕上抛"应为"抬腕上抛"；
4. "秦家依/秦嘉一"两种写法（交接说明已提醒），正式提交前统一。

## 六、结论与行动项

软件部分**方向合理、集成度中等**。上板前按优先级完成：

- **P0**：判定窗口全队定稿（±50/±100）；修复判定算法问题 #2、#4；统一前后端及文档三处判定参数；
- **P0**：接口契约三方统一（server.py / INTEGRATION.md / platform-adapter.js）；
- **P1**：accuracy 公式修正（问题 #3）；radar 与 metrics 语义拆分；
- **P1**：LVGL UI 迁移评估与排期（当前最大缺口）；
- **P2**：N-Back C 状态机、BLE 同步、音频链路补齐。
